[project]
name = "industrial-control-test-agent"
version = "0.1.0"
description = "工业控制测试代理框架基线"
authors = ["liujing4563-ctrl <noreply@example.com>"]
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"